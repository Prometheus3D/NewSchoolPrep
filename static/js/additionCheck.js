function checkAnswer() {
    var hidden_answer = document.getElementById('hidden-answer').value;
    var add_answer = document.getElementById('add-answer').value;
    let add_answer_int = Integer.parseInt(add_answer);
    console.log(add_answer_int)
}